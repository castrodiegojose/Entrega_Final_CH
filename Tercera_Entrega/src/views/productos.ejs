<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head.ejs');%>
    <script src="/socket.io/socket.io.js"></script>
    <title>PRODUCTOS</title>
</head>
<body>
    <div class="alert alert-success" role="alert">
        <p style ="font-size:larger">Bienvenido <%=user%> !</p> <a id="boton_logout" class="btn btn-primary" href="http://localhost:8080/logout" style = "float: right;">Logout</a>
    </div>

    <h1>Productos</h1>
    <form border="3">
        <div class="mb-3">
            <label for="formGroupExampleInput" class="form-label">Nombre</label>
            <input type="text" class="form-control" id="nombre" placeholder="Nombre">
        </div>
        <div class="mb-3">
            <label for="formGroupExampleInput" class="form-label">Precio</label>
            <input type="text" class="form-control" id="precio" placeholder="Precio">
        </div>
        <div class="mb-3">
            <label for="formGroupExampleInput" class="form-label">Foto</label>
            <input type="text" class="form-control" id="foto" placeholder="Foto">
        </div>
        <div class="mb-3">
            <label for="formGroupExampleInput" class="form-label">Descripcion</label>
            <input type="text" class="form-control" id="descripcion" placeholder="Descripcion">
        </div>
        <div class="mb-3">
            <label for="formGroupExampleInput" class="form-label">Codigo</label>
            <input type="text" class="form-control" id="codigo" placeholder="Codigo">
        </div>
        <div class="mb-3">
            <label for="formGroupExampleInput" class="form-label">Stock</label>
            <input type="text" class="form-control" id="stock" placeholder="Stock">
        </div>
        <button type="submit" id="agregar"class="btn btn-primary">Agregar</button>
    </form>
    <br/>
    
    <h1>Tabla Productos</h1>
    <table id="tabla" class="table table-dark table-striped-columns" border="3">
        <thead>
            <tr>
                <th scope="col">Producto</th>
                <th scope="col">Precio</th>
                <th scope="col">Foto</th>
                <th scope="col">Descripcion</th>
                <th scope="col">Codigo</th>
                <th scope="col">Stock</th>
            </tr>
        </thead>
        <tbody>
            <% if(productos.length === 0) { %>
                <tr>
                    <td id ="no hay datos" colspan="6">No hay datos</td>
                </tr> 
                <% } else { %>
                    <% for(let i = 0; i<productos.length; i++) { %>
                        <tr class="table table-dark table-striped-columns">
                            <td><%=productos[i].nombre %></td>
                            <td><%=productos[i].precio %></td>
                            <td>
                                <img src="<%=productos[i].thumbnail%>" size="5" alt="5">
                            </td>
                            <td><%=productos[i].descripcion %></td>
                            <td><%=productos[i].codigo %></td>
                            <td><%=productos[i].stock %></td>
                        </tr>
            <% }}%> 
        </tbody>
    </table>
    <br/>
    <a id="boton_logout" class="btn btn-primary" href="/" style = "float: right;">Volver al Carrito</a>
    
</body>
<script src="../public/main.js" charset="utf-8"></script>
</html>